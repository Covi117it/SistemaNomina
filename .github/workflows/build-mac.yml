name: Build macOS Release & Auto-Update

on:
  push:
    branches: [ main, master ]
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-mac:
    runs-on: macos-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install .NET 10 SDK
        run: |
          curl -sSL https://dot.net/v1/dotnet-install.sh | bash -s -- --version 10.0.110
          echo "$HOME/.dotnet" >> $GITHUB_PATH
          echo "DOTNET_ROOT=$HOME/.dotnet" >> $GITHUB_ENV

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-apple-darwin, aarch64-apple-darwin

      - name: Build .NET Backend for Intel & Apple Silicon macOS
        run: |
          cd backend
          $HOME/.dotnet/dotnet publish -c Release -r osx-x64 --self-contained true -p:PublishSingleFile=true
          $HOME/.dotnet/dotnet publish -c Release -r osx-arm64 --self-contained true -p:PublishSingleFile=true

      - name: Build Tauri Desktop App for Intel Mac (x86_64)
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
        run: |
          cd sistema-nomina
          pnpm install
          pnpm tauri build --target x86_64-apple-darwin

      - name: Build Tauri Desktop App for Apple Silicon Mac (aarch64)
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
        run: |
          cd sistema-nomina
          pnpm tauri build --target aarch64-apple-darwin

      - name: Upload macOS Bundle Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: SistemaNomina-macOS
          path: |
            sistema-nomina/src-tauri/target/x86_64-apple-darwin/release/bundle/dmg/*.dmg
            sistema-nomina/src-tauri/target/aarch64-apple-darwin/release/bundle/dmg/*.dmg
            sistema-nomina/src-tauri/target/release/bundle/dmg/*.dmg

      - name: Create or Update GitHub Release for Auto-Updater
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v0.1.1
          name: Release v0.1.1 (Actualización MariaDB 24/7 Nube Aiven)
          draft: false
          prerelease: false
          files: |
            sistema-nomina/src-tauri/target/**/release/bundle/dmg/*.dmg
            sistema-nomina/src-tauri/target/**/release/bundle/macos/*.app.tar.gz*
            sistema-nomina/src-tauri/target/**/release/bundle/latest.json
